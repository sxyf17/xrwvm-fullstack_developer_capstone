to use:

open docker desktop
type following commands:
docker build . -t nodeapp    
docker-compose up 

then navigate to localhost:3030 (or whatever the port is) and specify endpoint
ex:
localhost:3030/fetchDealers